select 
	sqltext.text,
	req.session_id,
	req.status,
	req.command,
	req.cpu_time,
	req.total_elapsed_time,
	db.name database_name
from 
	sys.dm_exec_requests req cross apply 
	sys.dm_exec_sql_text(sql_handle) as sqltext inner join
	sys.databases db on (req.database_id = db.database_id)